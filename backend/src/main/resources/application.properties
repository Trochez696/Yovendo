# Nombre interno con el que Spring identifica la aplicacion.
spring.application.name=backend

# Conexion local a MySQL. Ajustar usuario, contrasena y base de datos segun el entorno.
spring.datasource.url=jdbc:mysql://localhost:3306/yovendo
spring.datasource.username=root
spring.datasource.password=jeyson1171

# Hibernate actualiza el esquema automaticamente y muestra las consultas SQL en consola.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configuracion JWT: secreto base64 y tiempo de expiracion en milisegundos.
jwt.secret=Y292ZW5kb19zZWNyZXRfa2V5X3BhcmFfZWwtcHJveWVjdG8tMjU=
jwt.expiration=86400000
